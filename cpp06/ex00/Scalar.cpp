#include "Scalar.hpp"


void    isChar(char *input, t_nb *number)
{
    std::string 
    if(std::length(input) == 1)
        std::cout << "Maybe a char" << std::endl;
}

void    numberType(char *input, t_nb *number)
{
    isChar(input, number);

}

void    numbering(char *input)
{
    t_nb    number;

    numberType(input, &number);
    std::cout << input << std::endl;
}